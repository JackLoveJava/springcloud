server:
  port: 8483

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: replica1
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://replica2:8583/eureka/
